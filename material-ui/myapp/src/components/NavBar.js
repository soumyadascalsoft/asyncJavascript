import React from 'react'
import {AppBar} from '@mui/material';

function NavBar() {
  return (
    <div>
        <AppBar position='fixed' color='secondary'> Company Name </AppBar>
    </div>
  )
}

export default NavBar